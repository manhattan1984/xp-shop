import React from "react";

const Loading = () => {
  return (
    <div className="animate-pulse bg-white text-black h-screen flex justify-center items-center">Loading...</div>
  );
};

export default Loading;
